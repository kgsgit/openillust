# Node modules
node_modules/

# Next.js 빌드 결과물
.next/
out/           # next export 로 정적 사이트를 내보낼 때
build/         # (프로젝트에 따라 달라질 수 있음)

# Netlify 로컬 설정
.netlify/*
!/.netlify/functions-internal  # Netlify Functions는 추적해야 함
netlify.toml   # (원본으로 관리하길 원치 않는다면)

# 환경 변수
.env.local
.env.*.local
.env.*         # .env.production 등

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 테스트 커버리지
coverage/

# OS별 시스템 파일
.DS_Store
Thumbs.db

# 에디터 / IDE 설정 (VSCode 예시)
.vscode/
.idea/

# 기타
.cache/
